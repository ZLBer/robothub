spring:
  application:
    name: hubservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true

  datasource:
    driver-class-name: com.mysql.jdbc.Driver

#      discovery:
#        enabled: true
#        serviceId: configserver

